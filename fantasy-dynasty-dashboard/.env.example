# Base URL of the deployed Python VOR backend (see /fantasy-backend).
# Leave unset and the Roster Health tab reports that the backend is
# unconfigured rather than showing numbers it cannot compute.
VITE_API_BASE_URL=https://your-backend.onrender.com
