# Base URL of the deployed Python analytics backend (see /backend).
# Leave unset and the Roster Value / Waiver Wire tabs report that the
# backend is unconfigured rather than showing numbers they cannot compute.
VITE_API_BASE_URL=https://your-backend.onrender.com
